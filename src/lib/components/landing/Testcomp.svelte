<!-- <script>
    import { onMount } from "svelte";

    let done = false;
    onMount(() => {
        const p = document.getElementById("parent");
        const each_txt = p.innerText.split("");
        console.log(each_txt, p.innerText);
        let pp;
        for (let i = 0; i < each_txt.length; i++) {
            pp +=
                "<span style='--time-index:" +
                i +
                "s' class='animate-entry-text'>" +
                each_txt[i] +
                "</span>";
        }
        p.innerHTML = pp;
    });
</script>

<div id="parent" class="_text-entery-parent text-5xl">Oluwatobi</div> -->
